# Custom-Git-Implementation
A lightweight, from-scratch implementation of the Git core version control system written in Python. This project completely cuts out external dependencies to reconstruct Git's internal object model, content-addressable storage filesystem, and remote synchronization network protocols [gitprotocol-pack, gitformat-pack].
